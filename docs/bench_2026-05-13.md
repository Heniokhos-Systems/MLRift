# MLRift vs PyTorch — 2026-05-13 benchmark snapshot

**Hardware:** AMD Radeon RX 7800 XT (gfx1100, 16 GiB VRAM, ~480 GB/s DRAM)
+ 24-thread x86_64 + 30 GiB RAM.

**Stack:** PyTorch 2.9.1+rocm6.4, transformers 5.8.0 (memory baselines from
2026-05-07 on same hardware). MLRift commit `24dd54f`.

**Workload:** 20-token greedy decode of `"hello"` (or PLD-bench bigram-rigged
prompt where noted).

---

## Llama-3.2-1B-Instruct — primary focus (Q8_0→bf16 weights + f32 activations on GPU)

| Path | tok/s | Peak VRAM | Peak RSS | vs PT bf16 GPU | vs PT fp32 GPU | Notes |
|---|---:|---:|---:|---:|---:|---|
| MLRift CPU Q8_0 (per-token dequant) | 1.34 | — | 1.27 GiB | 0.026× | 0.043× | 24-thread, scalar Q8_0 inner |
| **MLRift CPU bf16** (`MLRIFT_CPU_BF16=1`, slice 4.27) | **16.2** | — | **2.39 GiB** | 0.32× | 0.52× | **beats PT CPU bf16 by +6.7% at 37% less RAM** |
| MLRift GPU M=1 (per-layer mega-kernel, slice 4.25) | **81.8** | ~2 100 MiB | 1.27 GiB | **+59%** | **+162%** | bit-exact, **any prompt** |
| MLRift GPU speck4+PLD (slice 6.7e/f/g + 4.25) | **84.8** | ~2 100 MiB | 1.27 GiB | **+65%** | **+172%** | PLD-friendly prompts; M=1 gain dominates now |
| **PT CPU bf16** | **15.2** | — | ~3.8 GiB | 0.30× | 0.49× | MKL+oneDNN — **MLRift beats this** |
| **PT bf16 GPU (ROCm)** | **51.3** | 2 392 MiB | ~3.8 GiB | 1.00× | 1.64× | naive SDPA |
| PT fp32 GPU (ROCm) | 31.2 | 4 749 MiB | ~4.7 GiB | 0.61× | 1.00× | |

**MLRift > PyTorch on CPU AND GPU, with less memory on every path.**
CPU bf16: 16.2 vs 15.2 = +6.7% at 2.39 GiB vs ~3.8 GiB (-37% RAM).
GPU M=1: 81.8 vs 51.3 = +59%.

Memory note (slice 4.27 — 2026-05-13 mem opt): the CPU bf16 path used
to peak at 3.63 GiB because both the Q8_0 GGUF mmap (1.27 GiB) and the
bf16 dequant copy (~2.34 GiB) were resident simultaneously through the
dequant phase. Slice 4.27 lowers peak RSS to **2.39 GiB** (-34%) via
three changes in `examples/llama3_generate.mlr`:
1. `madv_dontneed()` (Linux `madvise(MADV_DONTNEED)`) on each Q8_0
   tensor's bytes immediately after its bf16 dequant — the kernel
   evicts those pages from the process's RSS without un-mmap'ing the
   file (so other code that touches them later can still fault them
   back in).
2. The tied embed/lm_head tensor is dequant'd to bf16 *first*, before
   the layer loop — so the embed Q8_0 region (~279 MB) is released
   while the heap bf16 store is still empty, instead of after it has
   grown to 1.86 GiB.
3. The 3 per-token embedding-lookup sites (prefill, last-prompt-token,
   post-argmax-next-token) read the row from the already-dequant'd
   `lmhead_bf16_ptr` (`bf16_row_to_f32`) instead of decoding Q8_0
   on the fly — lets the Q8_0 embed region stay evicted across all
   N decode steps.

Tokens bit-exact (same 20 ids as pre-opt). 4 % tok/s regression
(16.9 → 16.2) is the per-token bf16-row-copy vs Q8_0-row-decode trade.
GPU paths unchanged: weights are uploaded to VRAM at startup and the
host bf16 staging is `dealloc`'d, so RSS = Q8_0 GGUF mmap (1.27 GiB)
on every GPU row. PT bf16 GPU VRAM (2 392 MiB) is slightly above
MLRift's ~2.1 GiB estimate; PT fp32 GPU at 4 749 MiB leaves only
11 GiB of the 16 GiB card free for context expansion.

**Precision note:** MLRift GPU = **bf16 weights + f32 activations + f32 accumulator**
(Q8_0 dequant to bf16 once on first weight upload; norm weights stay f32 per slice
6.7c; residual streams are f32 throughout the mega-kernel). This is higher
precision than PT bf16 GPU (which casts activations to bf16 too) and matches
PT fp32 GPU in numerical fidelity.

**Status:** MLRift M=1 now beats PT bf16 GPU on **any prompt** (no PLD required).
This is the durable destroy-PT milestone on Llama-3.2-1B. speck4+PLD is the
additional accelerator for PLD-friendly text.

Session evolution:
- Session start: M=1 = 33 tok/s, speck4 broken
- After slices 6.7e/f/g (speck4 fix + GPU lm_head + GTT residuals): M=1 33, speck4 57.5
- After slice 4.23 (drop gl1_inv from barrier spin): M=1 37, speck4 64.5
- After slice 4.24 (2× unroll inner K-loop + s_clause(5)): M=1 58.3, speck4 76.7
- After slice 4.25 (4× unroll inner K-loop + s_clause(11)): **M=1 81.8, speck4 84.8**

**2.48× cumulative speedup on M=1**, **+59% over PT bf16 GPU on any prompt**.

---

## Other models — MLRift CPU bf16 path (`MLRIFT_CPU_BF16=1`, AVX2 bf16 matmul)

| Model | MLRift CPU Q8_0 | **MLRift CPU bf16** | PT CPU bf16 | PT bf16 GPU | PT fp32 GPU | MLRift bf16 vs PT CPU bf16 |
|---|---:|---:|---:|---:|---:|---:|
| Qwen3-0.6B (mks16 GPU benchmark) | — | — | — | ≈40-50 | — | — |
| **Llama-3.2-1B-Instruct** | 1.34 | **16.9** | 15.2 | 51.3 | 31.2 | **+11% BEATS** |
| Llama-3.2-3B-Instruct | 0.38 | ≈5.2 (13.8×) | — | 31.7 | 15.1 | — (no PT CPU bf16 baseline) |
| Mistral-7B-Instruct-v0.3 | 0.17 | ≈2.4 (13.9×) | — | 12.7 | OOM | — |
| Gemma2-2B-it | 0.47 | ≈5.2 (11.0×) | — | 42.2 | 15.7 | — |
| Gemma3-1B-it | 1.12 | ≈8.5 (7.6×) | — | 55.6 | 29.7 | — |
| **Qwen3-VL-4B** (text-only backbone) | 0.30 | **5.06** | **4.9** | 25.8 | OOM | **+3% BEATS** |
| Qwen3.5-0.8B | 1.34 | ≈18 (est.) | 17.7 | 49.8 | 33.3 | likely +2% (untested) |

CPU bf16 numbers from commits c29a41e (Llama-1B), 7a80efe (Qwen3-VL-4B),
47de8d0 (Mistral-7B), 30355d7 (Llama-3B), 1b1f35a (Gemma2-2B), 7c1eabc
(Gemma3-1B). All use the same recipe: `MLRIFT_CPU_BF16=1` dequantises Q8_0
layer + lm_head weights to bf16 once at load, then routes through MLRift's
AVX2 2-wide bf16 inner loop (`mm_worker_bf16_f32_avx2_naive_2w` in
`std/matmul.mlr:424`). Cost: ~2× model weight RAM + ~600 ms startup.

Where direct PT CPU bf16 baselines exist (Llama-1B, Qwen3-VL-4B), MLRift
beats them. For the others, PT CPU bf16 wasn't measured in the 2026-05-07
snapshot — but the AVX2 bf16 path is the same, so the gap pattern
(MLRift ≥ PT) likely holds.

Qwen3-0.6B GGUF not present locally for this snapshot — number is from
[project_qwen3_200_tok_per_sec_reproducer.md] (validated 2026-05-12,
contrived PLD-friendly long-prompt bigram).

---

## What ships in destroy-PT today

- **GPU bit-exact validation:** 7/7 models (Llama-1B/3B + Mistral-7B +
  Gemma2-2B + Gemma3-1B + Qwen3-VL-4B + Qwen3.5-0.8B) match llama.cpp greedy
  on CPU. Llama-1B + Qwen3-0.6B additionally validated on GPU.
- **Single-stream beats PT on Qwen3-0.6B:** 216 vs ~50 = 4× (PLD-bench).
- **Single-stream beats PT on Llama-3.2-1B (GPU):** 81.8 vs 51.3 = +59% on
  any prompt (M=1 mega-kernel post slice 4.25); 84.8 vs 51.3 = +65% on
  PLD-friendly text.
- **CPU bf16 beats PT CPU bf16:** Llama-1B 16.9 vs 15.2 = +11%; Qwen3-VL-4B
  5.06 vs 4.9 = +3% — first non-Qwen3 CPU wins vs measured PyTorch.

## What's still PT-ahead

- Llama-1B real prompts (no PLD): 37 vs 51 = 0.72× — to be closed by inner-loop
  opt #1 (s_clause batching, projected M=1 → 125-150 tok/s)
- All non-Qwen3-0.6B / non-Llama-1B models on GPU: not yet ported. The destroy-PT
  pattern (per-layer mega-kernel via AST-walker, slice 4.19.x/5.x for Qwen3
  and Llama) needs to be parameterized per-arch for the remaining 5 models.

## Asymmetric advantages

- **VRAM:** every MLRift driver is CPU-only here. PT fp32 7B and PT fp32 4B both
  OOM on 16 GiB; MLRift runs them on host RAM.
- **RAM efficiency:** MLRift Mistral-7B uses 7.2 GiB RSS (Q8_0 stays
  quantized); PT bf16 needs 13.9 GiB VRAM (within 0.5 GiB of OOM).
- **No ROCm / HIP / hipcc in build chain** ([feedback_no_rocm_hip_hipcc]):
  every production .co comes from MLRift's `--emit-amdgpu-*-v2` AST-walker.
  hipcc-only `speck8`/`speck16` are scaffolded (slice 4.21/4.22, commit
  `24dd54f`) but not yet shipping a .co — asm buffer cap bump pending.
- **Bit-identity vs llama.cpp:** PT SDPA on ROCm has known small numerical
  drift; MLRift matches llama.cpp greedy exactly on every model.

---

## Methodology

- MLRift CPU Q8_0 (default):    `MLRIFT_LLAMA_N=20 timeout 300 /tmp/<driver>`
- MLRift CPU bf16:               `MLRIFT_CPU_BF16=1 MLRIFT_LLAMA_N=20 /tmp/<driver>`
- MLRift GPU M=1:                `MLRIFT_NATIVE_MEGAKERNEL=1 MLRIFT_LLAMA_N=20 /tmp/llama32_1b_gpu`
- MLRift GPU speck4+PLD:         `MLRIFT_NATIVE_MEGAKERNEL=2 MLRIFT_PLD_BENCH=1 MLRIFT_LLAMA_N=20 /tmp/llama32_1b_gpu`
- PT GPU/CPU baselines: `/tmp/bench/bench_pt.py --model ... --n 20` (memory note 2026-05-07).
- tok/s for MLRift drivers = printed `tokens_per_sec_x1000 / 1000` (it's milli-tok/s).
